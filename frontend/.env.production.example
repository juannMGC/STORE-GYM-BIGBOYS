# Copiar estas claves en Vercel → Project → Settings → Environment Variables (Production).
# Valores de ejemplo: API en Render (sin barra final).

BACKEND_URL=https://store-gym-bigboys-api.onrender.com
NEXT_PUBLIC_API_URL=https://store-gym-bigboys-api.onrender.com
