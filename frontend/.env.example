# URL base del backend Nest (sin barra final).
# Local: http://localhost:3001
# Producción (Vercel): https://store-gym-bigboys-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:3001

# Recomendado en Vercel: misma URL que arriba. next.config usa BACKEND_URL para rewrites /api → Nest.
# Si no existe, se usa NEXT_PUBLIC_API_URL.
# BACKEND_URL=http://localhost:3001
